apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Crossplane is installed via Helm by ArgoCD
# This kustomization provides the Helm values and any additional resources

resources: []

# Note: The actual Helm installation is configured in the ArgoCD Application
# See: apps/platform/crossplane.yaml
#
# For Crossplane XRDs and Compositions, add them to crossplane/ directory
# at repository root and reference via separate ArgoCD Application
