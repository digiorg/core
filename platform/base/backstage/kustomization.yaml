apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: backstage

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

# Note: backstage-secrets is created by scripts/local-setup.nu before ArgoCD sync.
# Do NOT add secrets here — ArgoCD would overwrite the generated passwords.
