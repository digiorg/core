apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - digiorg-ingress.yaml
  # OpenCost: a single Ingress routes /opencost through the oauth2-proxy. The
  # custom v1.120.4 UI image serves assets and the API natively under /opencost,
  # so the separate (unauthenticated) asset-bypass Ingress was removed
  # (Issue #272 Option B). Bootstrap-managed (local-setup.nu), NOT ArgoCD.
  - opencost-portal-ingress.yaml
  - harbor-ingress.yaml
