apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-rbac-cm
    app.kubernetes.io/part-of: argocd
data:
  # Default role assigned to authenticated users not matched by any group
  policy.default: role:readonly

  # RBAC policy rules (Casbin CSV format)
  # Syntax: p, <role/user>, <resource>, <action>, <object>, <effect>
  #         g, <user/group>, <role>
  policy.csv: |
    # ── Admin role ──────────────────────────────────────────────────────────────
    p, role:admin, applications,    *, */*, allow
    p, role:admin, applicationsets, *, */*, allow
    p, role:admin, clusters,        *, *,   allow
    p, role:admin, repositories,    *, *,   allow
    p, role:admin, projects,        *, *,   allow
    p, role:admin, accounts,        *, *,   allow
    p, role:admin, certificates,    *, *,   allow
    p, role:admin, gpgkeys,         *, *,   allow
    p, role:admin, logs,            get, */*, allow
    p, role:admin, exec,            create, */*, allow

    # ── Read-only role ──────────────────────────────────────────────────────────
    p, role:readonly, applications,  get, */*, allow
    p, role:readonly, clusters,      get, *,   allow
    p, role:readonly, repositories,  get, *,   allow
    p, role:readonly, projects,      get, *,   allow
    p, role:readonly, logs,          get, */*, allow

    # ── Group → role bindings ────────────────────────────────────────────────────
    # Adjust group names to match your IdP (OIDC groups claim)
    g, platform-admins,  role:admin
    g, platform-viewers, role:readonly

  # OIDC token scopes used to extract group membership
  scopes: "[groups, email]"
